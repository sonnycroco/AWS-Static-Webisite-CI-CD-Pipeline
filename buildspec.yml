version: 0.2

phases:
  build:
    commands:
      - echo "Nothing to build for static website"
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id E3I58CM1Z9OO5W --paths "/*"

artifacts:
  files:
    - '**/*'
  base-directory: '.'
